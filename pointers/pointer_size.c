#include <stdio.h>

int main()
{
        printf("\n size of int=%ld, sizeof pointer = %ld \n", sizeof(int), sizeof(int *) );

        printf("\n size of long long= %ld \n", sizeof(long ) );

        return 0;



}
  
